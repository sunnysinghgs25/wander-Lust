<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TourTom</title>
</head>
<body>
    <h4><%= updated %></h4>
    <h4><%= added %></h4>
    <h4><%= rd  %></h4>
    <h2>EveryThing</h2>

    <ol>
        <li><%= listings.title %></li>
        <li><%= listings.description %></li>
        <li> &#8377;<%= listings.price.toLocaleString("en-IN") %></li>
        <li><%= listings.location %></li>
        <li><%= listings.country %></li>
        <a href="/listing/<%= listings._id %>/edit">Edit</a>
        <form action="/listing/<%= listings._id %>?_method=DELETE" method="post">
            <button>DELETE</button>
        </form>
        <!-- <li> <img src="listings.image" alt=""> </li>         -->
    </ol>
    <hr>
    <h4>Leave a Review</h4>
    <form method="post" action="/listing/<%= listings._id %>/review">
        <label for="comment">Comment</label>
        <textarea name="review[comment]" id="comment" required ></textarea>
        <br> 
        <br>
        <label for="rating">Rating</label>
        <input type="range" min="1" max="5" id = "rating" name="review[rating]">
        <br>
        <br>
        <button> Submit </button>
    </form>
    <h4>All Review</h4>
    <ul><% for(review of listings.review) {%>
       <li>
        <p><%= review.comment %></p>
        <p>Rating: <%= review.rating %></p>
        <form action="/listing/<%= listings._id %>/review/<%= review._id %>?_method=DELETE" method="post">
            <button>Delete this review</button>
        </form>
        
        </li>
        <% } %>    
    </ul>
    
</body>
</html>